server:
  port: 8080

llm:
  default_model: mock-llm

mysql:
  host: localhost
  port: 3306
  user: root
  password: password
  database: ai_compute

redis:
  host: localhost
  port: 6379

auth:
  api_keys:
    - test-api-key

rate_limit:
  per_minute:20

